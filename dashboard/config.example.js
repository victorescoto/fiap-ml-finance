// config.js - Dashboard Configuration
// Copy this file to config.js and customize for your environment

window.API_BASE = "https://your-api-gateway-url.execute-api.region.amazonaws.com";

// Optional: Environment indicator
window.ENVIRONMENT = "production"; // or "development", "staging"

// Optional: Debug mode
window.DEBUG_MODE = false;